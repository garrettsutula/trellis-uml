id: carts
name: Carts
components:
  service:
    id: cartsService
    name: Carts Service
    type: Service
  db:
    id: cartsDb
    name: Carts Database
    type: Database
relationships:
  - source:
      $ref: "master-config.yaml#/components/service" 
    target:
      $ref: "#/components/db" 
  - source:
      $ref: "#/components/service" 
    target:
      $ref: "#/components/db" 