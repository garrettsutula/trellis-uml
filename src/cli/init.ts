export async function initializeProject() {
    // TODO
    // Accept project name as only arg
    // Create folder in current directory or throw error
    // Create diagrams subfolder structure
    // Create app.ts in main folder
    // Create index.ts in subfolders
    throw new Error("init command isn't implemented yet.");
}