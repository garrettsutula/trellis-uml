name: Reset Password Controller
description: Allows users to reset their passwords with a single use URL.
technology: Spring MVC Rest Contoller
relationships:
  - type: uses
    target:
      $ref: './Security-Component.yaml'
  - type: uses
    target:
      $ref: './Email-Component.yaml'