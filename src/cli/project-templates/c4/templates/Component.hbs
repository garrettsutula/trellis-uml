@startuml
title {{name}} Class Diagram

package {{name}} {
  {{#each elements}}{{! Render each class in main model}}
    {{type}} {{parent.id}}.{{id}} {

    }
  {{/each}}
  {{! Render each class in relationships to other Components}}
  {{#each elements}}{{#each relationships}}
  {{#if isExternalRef}}
    {{type}} {{parent.id}}.{{id}} {

    }
  {{/if}}
  {{/each}}{{/each}}
  {{! Render the relationships between classes}}
  {{#each elements}}{{#each relationships source=.}}
    {{source.parent.id}}.{{source.id}} --> {{relationship.target.parent.id}}.{{relationship.target.id}}: {{type}}
  {{/each}}
  {{/each}}
@enduml