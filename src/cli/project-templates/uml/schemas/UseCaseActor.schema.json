{
  "$id": "UseCaseActor.schema.json",
  "title": "Use Case Actor",
  "description": "An actor and the systems they use to perform one or more use cases.",
  "type": "object",
    "properties": {
      "actor": {
        "$ref": "Actor.schema.json"
      },
      "uses": {
        "type": "array",
        "items": {
          "$ref": "Component.schema.json"
        }
      }
    },
    "required": ["actor", "uses"]
}

