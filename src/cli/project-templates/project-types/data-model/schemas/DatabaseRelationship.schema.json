{
  "$id": "DatabaseRelationship.schema.json",
  "title": "Database Relationship",
  "type": "object",
  "properties": {
    "sources": {
      "type": "array",
      "items": {
        "$ref": "Database.schema.json"
      }
    },
    "source": {
      "$ref": "Database.schema.json"
    },
    "target": {
      "$ref": "Database.schema.json"
    },
    "id": {
      "type": "string"
    },
    "name": {
      "type": "string"
    },
    "description": {
      "type": "string"
    },
    "type": {
      "type": "string",
      "enum": ["Replication", "Extract", "Load", "Access"]
    },
    "technology": {
      "type": "string"
    }
  },
  "required": ["name"],
  "oneOf": [
    {
      "required": ["source", "target", "type"]
    },
    {
      "required": ["sources", "target", "type"]
    }
  ]
}